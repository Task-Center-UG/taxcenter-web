services:
  taxcenter-web:
    image: ${IMAGE_NAME:-ghcr.io/task-center-ug/taxcenter-web}:${IMAGE_TAG:-latest}
    container_name: taxcenter-web
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "127.0.0.1:3000:3000"
